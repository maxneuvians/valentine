<.header>
  Listing Threats
  <:actions>
    <.link navigate={~p"/workspaces/#{assigns[:workspace_id]}/threats/new"}>
      <.button>New Threat Statement</.button>
    </.link>
  </:actions>
</.header>

<.table
  id="threats"
  rows={@streams.threats}
  row_click={fn {_id, threat} -> JS.navigate(~p"/workspaces/#{assigns[:workspace_id]}/threats/#{threat}") end}
>
  <:col :let={{_id, threat}} label="Name"><%= threat.threat_source %></:col>
  <:action :let={{_id, threat}}>
    <div class="sr-only">
      <.link navigate={~p"/workspaces/#{assigns[:workspace_id]}/threats/#{threat}"}>Edit</.link>
    </div>
  </:action>
  <:action :let={{id, threat}}>
    <.link
      phx-click={JS.push("delete", value: %{id: threat.id}) |> hide("##{id}")}
      data-confirm="Are you sure?"
    >
      Delete
    </.link>
  </:action>
</.table>